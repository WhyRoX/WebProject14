
<Section>
    <div id="title-coach-page">
         <div class="container-court">
                {{#if errors}}
                <div class="alert alert-danger" role="alert">
                    {{errors}}
                </div>
                {{/if}}
                    <h3>Liste des Coachs de l'équipe Vinci Court</h3>
    </div>


<div class="container-box">
    {{#each coaches}}
    <div class="design-mid">
        {{#if (eq status "coach")}}
            <td><a href="/coaches/details?user_id={{user_id}}">{{firstname}}  {{surname}}</a></td>
              
             <h6>Son adresse mail : {{email}}</h6>
             
            
        {{/if}}
        </div>

    {{/each}}
    </div>

</section>